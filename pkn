<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover" />
<title>Happy Birthday Priya 🎉</title>
<style>
html, body {
 height: 100%;
 margin: 0;
 padding: 0;
 background: #000;
 color: #fff;
 font-family: "Poppins", system-ui, sans-serif;
 text-align: center;
 -webkit-font-smoothing: antialiased;
 -webkit-user-select: none;
 overflow-x: hidden !important;
}
/* Background glow */
body::before {
 content: "";
 position: fixed;
 inset: 0;
 background: radial-gradient(circle at 20% 20%, rgba(255,90,150,0.28), transparent 60%),
             radial-gradient(circle at 80% 30%, rgba(255,255,255,0.20), transparent 70%),
             radial-gradient(circle at 50% 80%, rgba(255,40,120,0.20), transparent 60%);
 z-index: -2;
 animation: bgFloat 12s infinite alternate ease-in-out;
}
@keyframes bgFloat { 0% { transform: translateY(0); } 100% { transform: translateY(-25px); } }
/* ------------------ SOFT GLOW FIREWORKS (CSS ONLY) ------------------ */
.fireworks {
 position: fixed;
 inset: 0;
 z-index: -3 !important;
 pointer-events: none;
 overflow: hidden;
}
.firework {
 position: absolute;
 width: 6px;
 height: 6px;
 background: radial-gradient(circle, rgba(255,200,255,1), rgba(255,200,255,0));
 border-radius: 50%;
 animation: firework-explode 2.4s ease-out forwards;
 opacity: 0;
}
/* pastel colors */
.firework:nth-child(3n)     { background: radial-gradient(circle, rgba(255,180,220,1), rgba(255,180,220,0)); }
.firework:nth-child(3n + 1) { background: radial-gradient(circle, rgba(180,220,255,1), rgba(180,220,255,0)); }
.firework:nth-child(3n + 2) { background: radial-gradient(circle, rgba(210,255,210,1), rgba(210,255,210,0)); }
@keyframes firework-explode {
 0%   { transform: translate(0,0) scale(0.4); opacity: 1; }
 60%  { opacity: 0.9; }
 100% {
   transform: translate(var(--dx), var(--dy)) scale(0.1);
   opacity: 0;
 }
}
/* INTRO overlay */
#intro {
 position: fixed;
 inset: 0;
 display: flex;
 flex-direction: column;
 justify-content: center;
 align-items: center;
 padding: 20px;
 background: rgba(0,0,0,0.86);
 backdrop-filter: blur(6px);
 transition: opacity .45s ease;
 z-index: 20;
}
#intro.hidden { opacity: 0; pointer-events: none; visibility: hidden; }
#intro input, #intro button {
 width: 90%;
 max-width: 330px;
 padding: 12px 16px;
 border-radius: 12px;
 border: none;
 margin-top: 10px;
 font-size: 1rem;
}
#intro input { background: #fff; color: #111; letter-spacing: 6px; text-align: center; }
#intro button { background: linear-gradient(90deg,#ff3b8f,#ff7bcb); color: #fff; font-weight: 600; }
#intro .reassure { margin-top: 18px; color: #e6e6e6; font-size: 1rem; }
/* MAIN PAGE */
#main {
 display: none;
 flex-direction: column;
 justify-content: center;
 align-items: center;
 min-height: 100dvh;
 padding: 40px 16px 60px;
 box-sizing: border-box;
 overflow-y: auto;
 position: relative;
 z-index: 5;
}
h1 { font-size: 2rem; margin: 5px 0 10px; text-shadow: 0 6px 20px rgba(255,59,143,0.45); }
.subtext { font-size: 1.1rem; margin-bottom: 15px; }
.slideshow {
 width: 82vw;
 max-width: 360px;
 border-radius: 18px;
 overflow: hidden;
 margin-top: 10px;
 box-shadow: 0 0 30px rgba(255,59,143,0.35);
}
.slideshow img {
 width: 100%;
 height: auto;
 display: none;
 object-fit: contain;
}
.slideshow img.active { display: block; }
.wish { margin-top: 20px; font-size: 1rem; max-width: 90%; }
audio { display: none; }
</style>
</head>
<body>
<!-- FIREWORK LAYER -->
<div class="fireworks" id="fw"></div>
<!-- INTRO -->
<div id="intro">
<h2>Enter first 4 digits of your Phone Number</h2>
<input type="number" id="code" placeholder="_ _ _ _" />
<button onclick="checkCode()">Submit</button>
<p class="reassure">It’s completely safe 😊</p>
</div>
<!-- MAIN PAGE -->
<div id="main">
<h1>🎉 Happy Birthday Priya! 🎂</h1>
<p class="subtext">Wishing you laughter, cake, and endless fun! 🥳</p>
<div class="slideshow">
<img src="img1.jpg" class="active">
<img src="img2.jpg">
<img src="img3.jpg">
</div>
<p class="wish">Hey Priya! Hope your day is as bright and fun as you are 💕</p>
<audio id="bgMusic" src="song.mp3" preload="auto"></audio>
</div>
<!-- CONFETTI -->
<script src="https://cdn.jsdelivr.net/npm/canvas-confetti@1.6.0/dist/confetti.browser.min.js"></script>
<script>
const intro = document.getElementById("intro");
const main = document.getElementById("main");
const codeInput = document.getElementById("code");
const music = document.getElementById("bgMusic");
const slides = document.querySelectorAll(".slideshow img");
let slideIndex = 0;
function checkCode() {
 if (codeInput.value.trim() === "6363") {
   intro.classList.add("hidden");
   setTimeout(() => {
     intro.style.display = "none";
     main.style.display = "flex";
     document.body.style.overflow = "auto";
     startShow();
     startConfetti();
     startCSSFireworks();
   }, 400);
 } else {
   alert("Wrong code 😅 Try again!");
 }
}
function startShow() {
 music.play().catch(()=>{});
 setInterval(() => {
   slides[slideIndex].classList.remove("active");
   slideIndex = (slideIndex + 1) % slides.length;
   slides[slideIndex].classList.add("active");
 }, 3000);
}
/* CONFETTI TOP */
function startConfetti() {
 let end = Date.now() + 7000;
 (function frame() {
   confetti({
     particleCount: 5,
     spread: 70,
     startVelocity: 35,
     origin: { x: Math.random(), y: 0 }
   });
   if (Date.now() < end) requestAnimationFrame(frame);
 })();
}
/* SOFT CSS FIREWORKS */
function startCSSFireworks() {
 const layer = document.getElementById("fw");
 setInterval(() => {
   const particles = 22;
   const cx = Math.random() * window.innerWidth;
   const cy = Math.random() * window.innerHeight * 0.5;
   for (let i = 0; i < particles; i++) {
     const p = document.createElement("div");
     p.className = "firework";
     const angle = Math.random() * Math.PI * 2;
     const distance = 50 + Math.random() * 80;
     p.style.left = cx + "px";
     p.style.top = cy + "px";
     p.style.setProperty("--dx", Math.cos(angle) * distance + "px");
     p.style.setProperty("--dy", Math.sin(angle) * distance + "px");
     layer.appendChild(p);
     setTimeout(() => p.remove(), 2500);
   }
 }, 900);
}
codeInput.addEventListener("keydown", e => {
 if (e.key === "Enter") checkCode();
});
</script>
</body>
</html>
